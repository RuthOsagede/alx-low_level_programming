#include "main.h"

/**
 * _isupper - a function that check for uppercase
 * Return: return 1 if c is uppercase, otherwise return 0
 * @c: char to check
 */

int _isupper(int c)
{
	if (c >= 'A' &&  <= 'Z')
		return (0);
	else
		return (0);
}
